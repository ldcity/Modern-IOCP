# Repository Instructions: seamless-world

상위 `../AGENTS.md` 와 `../.ai/` 공용 하네스를 따른다. 추가로 이 프로젝트 고유 규범:

- **정본은 `ROADMAP.md`** 다. 작업 전에 해당 Phase 절을 읽는다. 충돌하면 로드맵이 이긴다.
- 커밋 규약(§8) · AI 보조 경계(§9) · 문서 템플릿(§10) 은 **로드맵이 정한다.** 다시 정의하지 않는다.
- 🔴 **역할 예외**: 이 프로젝트에서 **Claude 는 구현자가 아니라 조언자**다(`CLAUDE.md` 참조).
  설계 선택·불변식·authority 경계는 사용자가 결정한다.
- **Codex 의 역할은 변하지 않는다** — 독립 리뷰 + Git 저장소 작업 전담.
  리뷰 중 소스를 수정하지 않고 발견을 `../.ai/reviews/` 에 먼저 기록한다.
- 벤치 결과에는 **commit hash 와 실행 환경**을 반드시 포함한다(로드맵 §8).
- 주 1회 `docs/devlog/` 에 3~5줄. 못 한 주에는 "이번 주는 못 했다" 고 쓴다.
